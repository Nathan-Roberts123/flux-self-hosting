apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-install
  namespace: flux-system
spec:
  interval: 5m
  wait: true
  dependsOn:
    - name: infrastructure-config
  path: "./apps/development/install"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
